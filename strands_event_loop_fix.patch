--- a/.venv/lib/python3.12/site-packages/strands/event_loop/event_loop.py
+++ b/.venv/lib/python3.12/site-packages/strands/event_loop/event_loop.py
@@ -383,6 +383,16 @@ async def _handle_tool_execution(
 
     # Store parent cycle ID for the next cycle
     invocation_state["event_loop_parent_cycle_id"] = invocation_state["event_loop_cycle_id"]
+
+    # FIX: Ensure tool results are ordered to match tool_uses order
+    # In concurrent execution, results may complete in any order (race condition)
+    # but Bedrock requires results in the same order as the original tool uses
+    if len(tool_results) > 1:
+        tool_use_id_to_result = {result["toolUseId"]: result for result in tool_results}
+        ordered_tool_results = [
+            tool_use_id_to_result[tool_use["toolUseId"]]
+            for tool_use in tool_uses
+            if tool_use["toolUseId"] in tool_use_id_to_result
+        ]
+        tool_results[:] = ordered_tool_results
 
     tool_result_message: Message = {
         "role": "user",

